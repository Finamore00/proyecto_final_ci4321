add_library(ui STATIC ui_component.cpp)
add_subdirectory(font)
add_subdirectory(sprites)
target_include_directories(ui PUBLIC ${CMAKE_CURRENT_SOURCE_DIR})
target_link_libraries(ui PUBLIC shader scene_graph)